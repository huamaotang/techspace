# 数据类型和值
```
1、计算机程序是通过操作值来运行的，能够表示并操作的值的类型称为数据类型
2、javascript允许三种基本的数据类型－－数字、文本字符和布尔值，
	同时还支持两种小数据类型null（空）和undefined（未定义），他们各自只定义了一个值。
	除此之外，还支持符合数据类型－－对象（object）。
3、对象有两种：
    1）已命名的值的无序集合
    2）有编号的值的有序集合，即数组
4、从根本上说，对象和数组属于同一种数据类型，但是它们的行为完全不同，可看作是两种不同类型
5、特殊的对象－－函数（function），函数具有可执行代码的对象，可以通过调用函数来执行某些操作。
6、javascript语言的核心还定义了一些专用的对象。这些对象表示的不是新的数据类型，而是对象的新的类（class）。Date类定义的是表示日期的对象，RegExp类定义的是表示正则表达式的对象，Error类定义的是表示JavaScript程序中发生的语法错误和运行时错误的对象。
3.1 字符串

字符串直接量中的转义序列
\0   NULL字符串
\b   退格符
\t   水平制表符
\n   换行符
\v   垂直制表符
\f   换页符
\r   回车符
\"   双引号
\'   单引号或撇号
\\   反斜线符

字符串的使用
1、javascript的特性之一就是能够连接字符串
2、
    1) 字符串长度：s.length
    2) 字符串最后一个字符：s.charAt(s.length-1);
    3)抽出2、3、4个字符：s.substring(1,4);
    4)查找第一个字母“a”的位置：s.indexOf('a');

数字转化为字符串
1、添加一个空字符串：var str = n + "";
2、str = String(n);
3、str ＝ n.toString();
4、小数：
var a = 12234332.1111;
a.toFixed(0); //12234332
a.toFixed(2); //12234332.11

字符串转化为数字
1、'134234' - 0;（注意，不能加0，否则会字符串连接）
2、parseInt();
3.2 布尔值及类型转换

1、true false
2、Boolean();
3.3 函数

1、函数是一个可执行的Javascript代码段
2、是真正的数值
3、和数字、字符串一样的数据类型
3.4 对象

1、对象是已命名的数据集合。
如，image.heigth、image.width、document.write('aa')、image['heigth']等;
2、创建对象，
var o = new Object();
var now = new Date();
var obj = new object();
obj.x = 123;
obj.y = 'aa';
3、对象直接量
var obj = {x:123,y:'1'}
4、对象转换
toString()、valueOf()
3.5 数组

1、与对象一样，是数值的集合。
2、区别于对象，对象美国数值都是有名字的，数组每个数值有下标。
如，document.image[1].
       arr[1] = 11;
       arr[2] = {x:1,y:2}
    2) var arr = new Array(10);//创建指定量的数组
    3）var arr = new Array(1,2,3);
3.6 NULL

1、特殊值，表示‘无值’。
2、布尔：false 数字：0 字符串：null
3.7 undefined

1、使用未声明变量、使用已声明但没有赋值变量、使用不存在的对象属性，返回的就是这个值 2、不同于null，通过===或者typeof区分 a.b == null 返回 true a.b === null 返回 false 3、不是保留字

3.8 Date对象

3.9 正则表达式

3.10 Error对象